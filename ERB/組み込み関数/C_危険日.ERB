;=================================================
;   sub C_위험일
;=================================================
;   위험일
;-------------------------------------------------
;   対象が위험일か返す
;-------------------------------------------------
; Input:
;  ARG:0				キャラ
; Output:
;  RETURNF				真偽値
;-------------------------------------------------
@C_위험일(ARG = -1)
#FUNCTION
#DIM L_TMP
#DIM L_VAL

SIF ARG == -1
	ARG = TARGET

L_TMP = CFLAG:ARG:위험일
L_VAL = (DAY % 7)

;３日に一回위험일
L_TMP /= 2
L_VAL /= 2

IF TALENT:ARG:오토코
	RETURNF 0
ELSEIF L_TMP == L_VAL
	RETURNF 1
ELSE
	RETURNF 0
ENDIF

