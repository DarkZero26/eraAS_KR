;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	Module		:COMFxxx.ERB
;	Facility	:TRAINコマンド(73:아날바이브)。
;
;	Licence		:ライセンスフリー。
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;===============================================================================
;  ◇記載関数一覧(記載順)
;
;		Module Name						Explanation
;
;	・ COM_FUNC_GET_NAME_Xxxx			取得:コマンド名称
;	・ COM_FUNC_ABLE_Xxxx				CHK:コマンド実行条件
;	・ COM_FUNC_INPUT_Xxxx				コマンド実行確認
;	・ COM_FUNC_EXE_Xxxx				コマンド実行
;	・ COM_FUNC_SRC_Xxxx				コマンドソース
;	・ COM_FUNC_TRAIN_MSG_Xxxx			コマンドメッセージ
;	・ COM_FUNC_GET_TYPE_Xxxx			取得:コマンド種別
;
;===============================================================================

;=================================================
;   sub COM_FUNC_GET_NAME_Xxxx
;=================================================
;   取得:コマンド名称
;-------------------------------------------------
; Output:
;  RESULTS				コマンド名称
;-------------------------------------------------
@COM_FUNC_GET_NAME_X73
RESULTS = 애널바이브

;=================================================
;   sub COM_FUNC_ABLE_Xxxx
;=================================================
;   CHK:コマンド実行条件
;-------------------------------------------------
; Output:
;  RESULT				真偽値
;-------------------------------------------------
@COM_FUNC_ABLE_X73

;特定アイテムを所有
SIF !ITEM:아날바이브
	RETURN 0

RETURN 1

;=================================================
;   sub COM_FUNC_INPUT_Xxxx
;=================================================
;   コマンド実行確認
;-------------------------------------------------
; Output:
;  RETURN				真偽値
;-------------------------------------------------
@COM_FUNC_INPUT_X73
#DIM L_P
#DIM L_T

L_P = PLAYER
L_T = TARGET

;-----------------------------
;- 事前処理
;-----------------------------
;CHK
SIF TEQUIP:L_T:Ａ器具 != [[TRAIN:아날바이브]]
	RETURN 1

TEQUIP:L_T:Ａ器具 = 0

;-----------------------------
;- 情景
;-----------------------------

PRINTFORML %CALLNAME%의 아누스에서 애널바이브를 뺐다.
PRINTFORM %타겟은% 
IF TALENT:음란도
	;Ａ경험180以上
	IF EXP:Ａ경험 > 180
		PRINT 비순과 입에서 침을 흘리면서, 엉덩이를 벌리고 있
	;Ａ경험140以上
	ELSEIF EXP:Ａ경험 > 140
		PRINT 촉촉해진 눈으로 간청하듯이 엉덩이를 흔들고 있
	;Ａ경험100以上
	ELSEIF EXP:Ａ경험 > 100
		PRINT 부족하다는 듯 엉덩이를 내밀고 있
	;それ以外
	ELSE
		PRINT 조금 아쉬운 듯이 엉덩이를 흔들고 있
	ENDIF
ELSE
	;Ａ경험40以上
	IF EXP:Ａ경험 > 40
		PRINT 엉덩이를 머뭇거리면서도, 무언가 안심한 표정을 짓고 있
	;Ａ경험20以上
	ELSEIF EXP:Ａ경험 > 20
		PRINT 강제적인 배설감에 뺨을 붉히고 있
	;それ以外
	ELSE
		PRINT 자극에 괴로운 표정으로 버티고 있
	ENDIF
ENDIF
PRINTFORML 다…….
WAIT


;解除だけの為、0返し
RETURN 0

;=================================================
;   sub COM_FUNC_EXE_Xxxx
;=================================================
;   コマンド実行
;-------------------------------------------------
@COM_FUNC_EXE_X73
#DIM L_P
#DIM L_T
#DIM L_TMP
#DIM L_I
VARSET LOCAL

L_P = PLAYER
L_T = TARGET

;-----------------------------
;COM別FLAGの設定
;-----------------------------

TEQUIP:L_T:Ａ器具 = [[TRAIN:아날바이브]]
TEQUIP:L_T:Ａ器具経過ターン = 0

L_TMP = TEQUIP:L_T:ポイント性交
IF L_TMP
	CALLFORM COM_FUNC_GET_TYPE_X{L_TMP}
	SIF RESULT == [[COM種別:Ａ性交系]]
		TEQUIP:L_T:ポイント性交 = [[TRAIN:無し]]
ENDIF

;-----------------------------
;ソース追加処理
;-----------------------------

;-----------------------------
;その他の処理
;-----------------------------


;=================================================
;   sub COM_FUNC_SRC_Xxxx
;=================================================
;   コマンドソース
;-------------------------------------------------
@COM_FUNC_SRC_X73
#DIM L_P
#DIM L_T
#DIM L_TMP
#DIM L_I
VARSET LOCAL

L_P = PLAYER
L_T = TARGET

;-----------------------------
;COM別FLAGの設定
;-----------------------------

;-----------------------------
;ソース追加処理
;-----------------------------

LOCAL:0 = 120
LOCAL:1 = 100
LOCAL:2 = 100

L_TMP=150
LOCAL:0 = LOCAL:0 * L_TMP / 100
LOCAL:1 = LOCAL:1 * L_TMP / 100
LOCAL:2 = LOCAL:2 * L_TMP / 100


;- ソース
SOURCE:L_T:쾌A += LOCAL:0
SOURCE:L_T:性行為 += LOCAL:1
SOURCE:L_T:露出 += LOCAL:2

;- TR:ソース(部位고통)
CALL TR_SOURCE_ADD_部位고통("Ａ감각", L_T)


;-----------------------------
;経験の処理
;-----------------------------

;-----------------------------
;その他の処理
;-----------------------------



;=================================================
;   sub COM_FUNC_TRAIN_MSG_Xxxx
;=================================================
;   コマンドメッセージ
;-------------------------------------------------
@COM_FUNC_TRAIN_MSG_X73

;- 挿入時
IF !TEQUIP:Ｖ器具経過ターン

	PRINTFORM %CALLNAME%의 
	PRINTFORML 국문에 애널바이브를 넣었다…….
	PRINTFORM %타겟은% 
	IF TALENT:음란도
		;Ａ경험180以上
		IF EXP:Ａ경험 > 180
			PRINT 녹아내린 표정으로 스스로 엉덩이를 벌리고 있
		;Ａ경험140以上
		ELSEIF EXP:Ａ경험 > 140
			PRINT 삽입을 애타게 기다리고 있
		;Ａ경험100以上
		ELSEIF EXP:Ａ경험 > 100
			PRINT 볼을 붉히며, 기대와 함께 엉덩이를 흔들고 있
		;それ以外
		ELSE
			PRINT 국혈에 주어지는 바이브의 자극에 몸을 떨고 있
		ENDIF
	ELSE
		;Ａ경험40以上
		IF EXP:Ａ경험 > 40
			PRINT 기대와 부정이 뒤섞인 듯한 복잡한 표정을 하고 있
		;Ａ경험20以上
		ELSEIF EXP:Ａ경험 > 20
			PRINT 부끄러움에 잠긴 얼굴을 숙이고 있
		;それ以外
		ELSE
			PRINT 부정한 구멍을 침범당하는 치욕과 굴욕감에 빠진 표정을 하고 있
		ENDIF
	ENDIF
	PRINTL 다…….
	PRINTFORM %CALLNAME%의 
	;濡れ
	SIF TCVAR:キャラ윤활
		PRINT 점액 범벅인 
	PRINTFORML 아누스에 바이브를 꽂았다.

;- 継続中
ELSE
	PRINTFORML %타겟은% 아누스에 박힌 바이브가 주는 쾌감에 달콤한 목소리를 내고 있다…….
ENDIF

WAIT


;=================================================
;   sub COM_FUNC_GET_TYPE_Xxxx
;=================================================
;   取得:コマンド種別
;-------------------------------------------------
; Output:
;  RESULT				コマンド種別
;-------------------------------------------------
@COM_FUNC_GET_TYPE_X73
RETURN [[COM種別:道具系]]

